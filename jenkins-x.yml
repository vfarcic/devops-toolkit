buildPack: go
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        replace: true
        steps:
        - command: |-
            hugo
            export VERSION=$PREVIEW_VERSION && skaffold build -f skaffold.yaml
