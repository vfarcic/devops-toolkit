buildPack: go
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        replace: true
        steps:
        - name: hugo
          command: hugo
        - name: compile
          command: export VERSION=$PREVIEW_VERSION && skaffold build -f skaffold.yaml
